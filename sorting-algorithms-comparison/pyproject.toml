[project]
name = "sorting-algorithms-comparison"
version = "0.1.0"
description = ""
authors = [
    {name = "linzizhou",email = "85533594+ejjeuej@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib (>=3.11.1,<4.0.0)",
    "numpy (>=2.5.1,<3.0.0)"
]

[tool.poetry]
packages = [{include = "sorting_algorithms_comparison", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
